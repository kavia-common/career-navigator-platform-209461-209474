# Frontend (Vite + React) - .env.example
# Copy to .env and set the backend API base URL.

# Preferred env var for Vite at build-time:
VITE_API_BASE=http://localhost:8000

# Compatibility fallback recognized by the app if VITE_* is not set:
REACT_APP_API_BASE=http://localhost:8000
